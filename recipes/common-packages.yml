# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json

type: dnf
repos:
  copr:
    enable:
      # - atim/starship
      - jdxcode/mise
install:
  packages:
    - papirus-icon-theme
    - mise
remove:
  packages:
    - virt-manager
    - hhd
    - hhd-ui
